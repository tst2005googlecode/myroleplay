MyRolePlay Database

MyRolePlay(curProfile, Identification, Appearance, Lore, Status, Character, GuildInfo, oocInfo, PetInfo, Achievements)

mrpPlayerList(Username)
	Username(hasInfo, curProfile, Identification, Appearance, Lore, Status, Character, GuildInfo, oocInfo, PetInfo, Achievements)
		Identification(prefix, firstname, middlename, surname, FamilyName, title, nickname, housename, Professions, dateOfBirth, deity)
			Professions(Id)
				Id(professionName, professionDescription, skillLevel, SkillList)
					SkillList(Id)
						Id(skillName, skillDescription, difficultyLevel, LinkedCreateItem, LinkedReagents)
							LinkedCreateItem(Item, itemName)
							LinkedReagents(Index)
								Index(Item, itemNames)
			FamilyName(Id)
				Id(familyName, RelativesList)
					RelativesList(Username)
						Username(RelativeName)
							RelativeName(firstName, middleName, surName, relativeType)
		Appearance(eyeColour, height, weight, apparentAge, currentEmotion, description)
		Lore(curHome, birthPlace, motto, history)
		Status(roleplay, character)
		Character(Race)
			Race(raceName, raceDescription)
		GuildInfo(GuildAllianceID, myGuildId)
			GuildAllianceID(guildAllianceName, GuildList, guildAllianceRank)
				GuildList()
		PetInfo()
		Achievements()